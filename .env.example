# Ottavia Agent Simulator — Server Environment

# MongoDB connection string (read-only access recommended)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/

# MongoDB database name
MONGODB_DB_NAME=ottavia

# Server port (default: 3002)
PORT=3002
